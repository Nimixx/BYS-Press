{#
  Header Component

  Complete header with optional top bar and main navigation
  - TopBar: Small bar with contact info, social links (optional)
  - HeaderBar: Main bar with logo and navigation
  - Desktop: Shows Menu.twig (horizontal menu)
  - Mobile: Shows MenuToggle Vue component + MobileMenu Vue component (sidebar)
  Menu items are pre-processed server-side with active states

  @param {object} site - Timber Site object
  @param {object} logo - Logo object with url and alt
  @param {array} menu_items - Menu items array
  @param {array} topbar_social_links - Optional social links for top bar
  @param {string} topbar_phone - Optional phone number for top bar
  @param {string} topbar_email - Optional email for top bar
  @param {string} topbar_message - Optional message for top bar
#}

<header class="site-header" role="banner">
  {# Top Bar - Small bar with additional info (optional) #}
  {% include 'components/Header/elements/TopBar/TopBar.twig' with {
    social_links: topbar_social_links,
    phone: topbar_phone,
    email: topbar_email,
    message: topbar_message
  } %}

  {# Header Bar - Main bar with Logo and Navigation #}
  {% include 'components/Header/elements/HeaderBar/HeaderBar.twig' with {
    site: site,
    logo: logo,
    menu_items: menu_items
  } %}

  {# Mobile Menu Sidebar - Vue Component #}
  {% if menu_items %}
    <div
      data-vue-component="MobileMenu"
      data-props='{"isOpen": false, "items": {{ menu_items|json_encode|raw }}}'
      data-eager
    ></div>
  {% endif %}
</header>
