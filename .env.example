# Environment Variables Example
# Copy this file to .env and fill in your values
# IMPORTANT: Only variables prefixed with VITE_ are exposed to client-side code

# ============================================
# Client-Side Variables (Exposed to Browser)
# ============================================
# These are embedded in your JavaScript bundle and visible to users
# Only use for non-sensitive, public configuration

# API Endpoints
VITE_API_URL=https://api.example.com
VITE_API_VERSION=v1

# Feature Flags
VITE_ENABLE_DEBUG=false
VITE_ENABLE_ANALYTICS=true

# Public Keys (client-side only)
VITE_GOOGLE_MAPS_API_KEY=
VITE_RECAPTCHA_SITE_KEY=

# Theme Configuration
VITE_THEME_VERSION=1.0.0
VITE_CDN_URL=

# ============================================
# Server-Side Variables (NOT exposed)
# ============================================
# These can only be used in vite.config.js or Node.js scripts
# Never exposed to the browser

# Build Configuration
NODE_ENV=development
BUILD_TARGET=modern

# ============================================
# SECURITY WARNING
# ============================================
# - NEVER put sensitive data in VITE_ prefixed variables
# - NEVER put API secrets, private keys, or passwords here
# - All VITE_ variables are PUBLIC and visible in browser
# - For WordPress secrets, use wp-config.php constants instead
