/**
 * useCounter Composable Export
 *
 * Barrel export for the useCounter composable and its types.
 *
 * @module composables/useCounter
 */

export { useCounter, useSimpleCounter } from './useCounter';
export type { CounterOptions, CounterReturn } from './useCounter.types';
